
== Netbeans Platform 


kriolos_obiz.conf
----
default_options="-J-DTopSecurityManager.disable=true -J-Djdk.net.URLClassPath.disableClassPathURLCheck=true -J--add-opens=java.base/java.net=ALL-UNNAMED -J--add-opens=java.base/sun.net.www.protocol.jar=ALL-UNNAMED"

----

== Maven compiler


----
<plugin>  
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-compiler-plugin</artifactId>
    <version>3.15.0</version>
    <configuration>
        <compilerArgs>
            <arg>-Xlint</arg>
        </compilerArgs>

        <annotationProcessorPaths>
            <!--
            <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </path>
            <path>
                <groupId>org.netbeans.api</groupId>
                <artifactId>org-openide-util</artifactId>
                <version>${netbeans.version}</version>
            </path>
            -->
            <!-- Critical for NetBeans Platform API annotations processing -->

            <!-- 2. NetBeans Common Annotations Processor (Crucial for @Messages  and Bundle generation) -->
            <path>
                <groupId>org.netbeans.api</groupId>
                <artifactId>org-netbeans-api-annotations-common</artifactId>
                <version>${netbeans.version}</version>
            </path>

            <!-- 3. NetBeans Utilities Processor (Crucial for layer generation/registrations) -->
            <path>
                <groupId>org.netbeans.api</groupId>
                <artifactId>org-openide-util</artifactId>
                <version>${netbeans.version}</version>
            </path>
        </annotationProcessorPaths>
    </configuration>
</plugin>
----




== 


----
# 1. Limpe qualquer rasto da tentativa anterior na cache do Git
git rm --cached kriolos-pos 2>/dev/null

# 2. Adicione o submodule com o URL completo e correto
git submodule add https://github.com/kriolos-obiz/kriolos-pos.git kriolos-pos

# 3. Sincronize e atualize os ficheiros locais
git submodule sync
git submodule update --init --recursive


./mw
----